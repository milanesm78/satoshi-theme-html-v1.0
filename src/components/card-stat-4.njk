<div class="card">
  <div class="card-body">
    <div class="d-flex gap-3 mb-1">
      <span class="text-muted">{{ title if title else 'Market Cap' }}</span>
      {% if badge %}
      <span class="badge text-bg-{{ badge.color if badge.color else 'light' }} rounded">{{ badge.value if badge.value else '24h' }}</span>
      {% endif %}
      {% if info %}
      <span class="text-muted text-opacity-60 text-opacity-100-hover" tabindex="0" data-bs-toggle="popover" data-bs-trigger="hover focus" data-bs-placement="right" data-bs-html="true" data-bs-content="The total market value of a cryptocurrency's circulating supply. It is analogous to the free-float capitalization in the stock market.<br /><br />Market Cap = Current Price x Circulating Supply." role="button">
        <i class="bi bi-info-circle"></i>
      </span>
      {% endif %}
    </div>
    <div class="d-flex align-items-center">
      <span class="text-lg text-heading fw-bold">
        {{ value if value else '$19,3M' }}
      </span>
      <span class="ms-auto text-{{ helper.color if helper.color else 'success' }} text-sm fw-semibold">
        {{ helper.value if helper.value else '+7.57%' }}
      </span>
    </div>
  </div>
</div>
